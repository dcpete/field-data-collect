const initialState = [
  { name: "Project 1" },
  { name: "Project 2" },
];

export default function ProjectsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
};